1.
자바는
컴파일러 - 사용자가 작성한 코드를 컴파일러에 의하여 컴퓨터가 이해할수 있도록 변환
코드를 읽을때 전체로 읽는다.
객체지향언어
jvm - 자바가 제공하는 인터프리터

자바스크립트
객체기반언어
내부에 인터프리터가 없어 코드를 읽을 때 한줄씩 읽어들인다.
웹브라우저가 해석하기때문에 속도가 느리다.


2.
os별로 환경이상이하기때문에 jdk개발 -> 

사용자가 코드를 입력하면 jdk가 컴파일하고
jdk가 다시 컴파일한 코드를 os에 인터프리팅한다.

이 중간과정때문에 느리다고 한다.

compile
java -> (javac) -> class로 변환 

runtime
class -> (java) -> class loader -> Bytecode verifier -> JITcode generator...

3.
자바의 기본단위 : 클래스