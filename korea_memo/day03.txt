레퍼런스타입

String s = new String("123") -레퍼런스타입
String s2 = "123" - 원시타입
string 둘다가능

클래스에는
day, month, year라는 값이있고
각각의 방이있다.
그방의 초기값은 0이고
클래스에 값을 넣으면 주소값이생긴다.

객체를 레퍼런스변수에 담으면 그 주소값 그대로 담고
다시 레퍼런스변수에 값을 담으면 주소값이 바뀐다.

레퍼런스타입은 주소값이 전달되고
원시타입은 값자체가 전달된다.

변수는 카멜표기법
상수는 언더스코어