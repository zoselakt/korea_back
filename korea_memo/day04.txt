이중배열 길이
for(int i=0; i<temp.length; i++){
 for(int j=0; j<temp[i].length; j++){
  sysout(temp[i][j]

사이즈를 확인할수 없을때 확인
자바 컬렉션..............

자식은 부모를 알지만
부모는 자식을 모른다.
자식객체는 부모객체에 의존하고
부모객체는

접근제어자 범위
public : 모든 패키지 접근가능
default : 같은 패키지에만 접근가능
protected : 같은 패키지의 클래스 또는 해당 클래스 상속받은 다른패키지의 클래스 접근가능
private : 해당 클래스에만 접근가능

